# Assessment: PrimeFaces 8.0 To 10.0.0

Estimate the cost, time, effort, and risk for upgrading only PrimeFaces from 8.0 to 10.0.0.

PrimeFaces does not have a normal public 9.x major stage in this migration path; note that in the report.

Do not change application files. The only allowed output is:

```text
aws-transform/eap-primefaces-modernization/reports/03-primefaces-10-assessment.md
```

The report must include `## Estimate`, `## Cost`, `## QA Focus`, `## Risks And Blockers`, and `## Recommendation`.

Cost must use `AWS Transform cost = estimated agent minutes * 0.035`.

Scope is PrimeFaces only. Do not include JBoss, Jakarta, Java, Docker, descriptor, or namespace migration in this estimate.
