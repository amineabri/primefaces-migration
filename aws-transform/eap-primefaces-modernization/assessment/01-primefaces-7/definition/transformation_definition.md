# Assessment: PrimeFaces 6.2.30 To 7.0

Estimate the cost, time, effort, and risk for upgrading only PrimeFaces from 6.2.30 to 7.0.

Do not change application files. The only allowed output is:

```text
aws-transform/eap-primefaces-modernization/reports/01-primefaces-7-assessment.md
```

The report must include `## Estimate`, `## Cost`, `## QA Focus`, `## Risks And Blockers`, and `## Recommendation`.

Cost must use:

```text
AWS Transform cost = estimated agent minutes * 0.035
```

Scope is PrimeFaces only. Do not include JBoss, Jakarta, Java, Docker, descriptor, or namespace migration in this estimate.
